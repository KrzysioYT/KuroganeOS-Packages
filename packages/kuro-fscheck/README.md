# kuro-fscheck

Checks the main KuroganeOS filesystem roots and counts installed `/apps` entries.

Install in Anvil and run `run kuro-fscheck` (or simply `kuro-fscheck` once direct `/apps` command resolution is available).
