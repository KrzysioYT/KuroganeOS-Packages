# hello-text

Minimal package used to prove the complete Anvil repository path.

It installs:

```text
/home/hello.txt
```

The package has no dependencies, peer requirements or conflicts. It is intentionally tiny so repository authors can copy its structure when learning `KPKG1`.
