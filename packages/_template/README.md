# Package template

Copy this directory to `packages/<package-name>/` and rename the payload path in `manifest.kpkg`.

Directories whose name starts with `_` are ignored by the repository validator and catalog generator, so this template may intentionally contain placeholder values.

Read `docs/CREATING_PACKAGE.md` before publishing.
