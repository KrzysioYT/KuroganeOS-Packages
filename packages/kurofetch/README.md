# kurofetch

Compact system overview for KuroganeOS. Shows PID, CPU/GPU/RAM/disk load, memory, uptime and current network state.

Install in **Anvil**. The package is built automatically by repository CI and installs `/apps/kurofetch`.

Run from Kurosh with:

```text
run kurofetch
```
